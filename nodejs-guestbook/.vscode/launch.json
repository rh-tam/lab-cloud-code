{
    "version": "0.2.0",
    "configurations": [
        
        {
            "name": "Run/Debug on Kubernetes",
            "type": "cloudcode.kubernetes",
            "request": "launch",
            "skaffoldConfig": "${workspaceFolder}/skaffold.yaml",
            "watch": true,
            "cleanUp": false,
            "portForward": true,
            "imageRegistry": "quay.io/red_hat",
        }
    ]
}
